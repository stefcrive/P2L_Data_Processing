add standards database page, that it is referenced for the standard measurments based calibration of raw data.

move export data to a new page

apply open save close session, autosave sesssion, add file to session, discard all changes

Add progress bar animation when importing 

add manual linearity override to standard measurements too
